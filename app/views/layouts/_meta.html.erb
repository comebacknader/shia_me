<meta name="keywords" content="shia, islam, shia marriage, shias, matchmaking, muslim,
		    marry, karbala, shia spouse " />
<meta name="description" content = "<%= metacontent %>" />
 
 <% if Rails.env.production? %>
  <%= tag :meta, :name => "stripe-key", :content => ENV['STRIPE_LIVE_PUB'] %>
 <% else %>
  <%= tag :meta, :name => "stripe-key", :content => STRIPE_PUBLIC_KEY %>
 <% end %>
 
 